使用Web 端方案实现跨平台并操控 CH340 的 DTR 和 RTS 以实现单片机 usb 自动烧写  像 flymcu 那样。一个无需安装、跨 Windows/Mac 平台、纯浏览器的 STM32 烧写工具。

# 基于 Web Serial 的 STM32 全功能烧录上位机开发规范
本规范旨在指导开发者使用 HTML + JavaScript（Web Serial API）开发出具备 **设备识别、全片擦除、固件烧写、数据校验、解除读保护** 等完整功能的 STM32 烧录工具（Web 版 FlyMcu）。
## 第一部分：硬件时序与物理电平真相
控制单片机进出 Bootloader 的本质，是利用串口的 `DTR` 和 `RTS` 引脚去精准控制 STM32 的 `BOOT0` 和 `RESET`。
### 1. 反相定律
无论是在 Python 还是 JS 的 API 中：
* **软件设为 True / 低电平** $\Rightarrow$ 物理引脚输出 **0V (低电平)**
* **软件设为 False / 高电平** $\Rightarrow$ 物理引脚输出 **3.3V (高电平)**
*(注：FlyMcu 界面上选的“低电平复位”，指的是软件层的低电平，即物理上的 0V)*
### 2. 两种主流电路与时序代码
为了兼容市面上 99% 的开发板，软件只需按照以下时序来操控引脚（无需在代码里区分这两种电路，因为它们的**物理效果殊途同归**）：
#### (1) 经典带三极管电路 (NPN + PNP 异或电路)
* **物理结构**：由一颗 NPN (如 S8050 控制 RESET) 和一颗 PNP (如 S8550 控制 BOOT0) 交叉组成。
* **物理效果**：只有当 DTR 和 RTS 电平**一高一低**时，三极管导通；**同高或同低**时，全部截止，引脚交由单片机默认电阻控制。
#### (2) CH340X 免外围直连电路
* **物理结构**：`DTR#` 直连 `BOOT0`，`RTS#` 直连 `RESET`。
#### 进出 Bootloader 的标准 JS 时序
无论上述哪种电路，**进入下载模式（BOOT0=1, RESET先0后1）**和**启动程序（BOOT0=0, RESET先0后1）**的代码是通用的。
```javascript
// 动作 1：进入 Bootloader (准备下载)
async function enterBootloader(port) {
    // 第一步：拉高 BOOT0，拉低 RESET（压死复位）
    await port.setSignals({ dataTerminalReady: false, requestToSend: true });
    await delay(100); // 必须等待足够长，让电容放电
    // 第二步：释放 RESET，同时保持 BOOT0 依然为高
    await port.setSignals({ dataTerminalReady: false, requestToSend: false });
    await delay(50);  // 等待 STM32 启动并进入内部 ISP 系统
}
// 动作 2：复位并运行用户程序 (下载结束)
async function resetToRun(port) {
    // 第一步：拉低 BOOT0，拉低 RESET
    await port.setSignals({ dataTerminalReady: true, requestToSend: true });
    await delay(100);
    // 第二步：彻底释放，程序起跑
    await port.setSignals({ dataTerminalReady: false, requestToSend: false });
}
// 通用延时函数
const delay = ms => new Promise(res => setTimeout(res, ms));
```
---
## 第二部分：STM32 官方 ISP 协议 (AN3155) 解析
与单片机成功建立物理连接后，接下来所有的交互都必须严格遵守 ST 官方的串口协议。
### 1. 通讯基础配置 
*   **波特率**：115200 (最稳)
*   **数据位**：8 bit
*   **停止位**：1 bit
*   **校验位**：**Even Parity（偶校验）** $\leftarrow$ 不设置偶校验单片机会直接无视所有指令
### 2. 指令格式与应答机制
上位机发送的每一条命令，单片机必定回复 1 个字节：
*   **ACK (成功)** = `0x79`
*   **NACK (失败/拒绝)** = `0x1F`
除握手指令外，所有指令发送时，都必须带上它的**异或反码**作为校验。例如，读取命令是 `0x11`，其反码是 `0xEE`（`0x11 ^ 0xFF = 0xEE`），就要连发 `[0x11, 0xEE]` 两个字节。

## 第三部分：对标 FlyMcu 的全功能代码实现
以下是将 FlyMcu 核心功能用 JavaScript 还原的逻辑流与伪代码。
### 前置工具函数准备
```javascript
let writer, reader;
// 发送字节数组
async function sendCmd(bytesArray) {
    await writer.write(new Uint8Array(bytesArray));
}
// 等待并读取指定长度的字节 (附带超时机制)
async function readBytes(length, timeout = 2000) {
    let result = new Uint8Array(length);
    let offset = 0;
    const timeoutPromise = new Promise((_, reject) => setTimeout(() => reject("Read Timeout"), timeout));
    async function readLoop() {
        while (offset < length) {
            const { value, done } = await reader.read();
            if (done) break;
            result.set(value, offset);
            offset += value.length;
        }
        return result;
    }
    return Promise.race([readLoop(), timeoutPromise]);
}
// 等待 ACK (0x79)
async function waitAck(timeout = 2000) {
    let res = await readBytes(1, timeout);
    if (res[0] === 0x1F) throw new Error("Received NACK (0x1F)");
    if (res[0] !== 0x79) throw new Error("Unknown response: " + res[0]);
    return true;
}
```
### 功能 1：同步握手 (Sync)
这是必须执行的第一步，让 STM32 侦测波特率。
*   **行为**：单发一个 `0x7F`，不带反码。
```javascript
await sendCmd([0x7F]);
await waitAck(1000); // 如果收到 0x79，说明连接大成功！
```
### 功能 2：读取芯片 ID (类似 FlyMcu 获取单片机型号)
*   **行为**：发送 `0x02` + `0xFD`。
*   **返回**：`ACK` + `字节数` + `PID (2字节)` + `ACK`。
```javascript
await sendCmd([0x02, 0xFD]);
await waitAck();
let lenBytes = await readBytes(1);
let pidBytes = await readBytes(lenBytes[0] + 1);
await waitAck();
console.log(`芯片 PID: 0x${pidBytes[0].toString(16)}${pidBytes[1].toString(16)}`);
```
### 功能 3：解除读保护与全片擦除 (排障功能)
如果 STM32 被锁，发送写入/擦除指令会直接返回 NACK。FlyMcu 会在此刻执行“解除读保护”，**这会自动引发长达 2 秒的全片清除**。
*   **行为**：发送 `0x92` + `0x6D`。
*   **返回**：立即回一个 `ACK`，接着芯片开始自我擦除。等擦除完毕（可能长达 2 秒），再回第二个 `ACK`，随后芯片自动硬件复位
```javascript
async function unprotectAndMassErase() {
    await sendCmd([0x92, 0x6D]);
    await waitAck(); // 收第一个 ACK
    console.log("正在执行全片清除，请等待 1~3 秒...");
    await waitAck(5000); // 等待极其漫长的第二个 ACK
    console.log("全片清除完成，芯片读保护已解除！");
    // 注意：解除读保护后芯片会自动硬件复位，你必须重新执行 enterBootloader() 和 Sync(0x7F)
}
```
### 功能 4：扇区擦除 / 全片擦除 (Erase)
如果芯片没锁，正常烧录前需要擦除。这里以拓展擦除 `0x44` 全片擦除为例。
*   **行为**：发送 `0x44` + `0xBB` $\Rightarrow$ 收到 ACK。
*   **发送全片擦除包**：发送 `[0xFF, 0xFF, 0x00]` （0xFFFF 代表全片擦除，0x00 是前两者的异或值） $\Rightarrow$ 收到 ACK。
```javascript
await sendCmd([0x44, 0xBB]);
await waitAck();
await sendCmd([0xFF, 0xFF, 0x00]);
await waitAck(3000); // 擦除耗时较长
console.log("擦除成功！");
```
### 功能 5：烧录固件 (Write Memory - 核心逻辑)
将 `.bin` 文件按每包最高 256 字节循环写入。通常 Flash 起始地址为 `0x08000000`。
每次写入分三步：发指令、发地址、发数据。
```javascript
// 辅助计算 XOR 校验和的函数
function calcXOR(dataArray) {
    return dataArray.reduce((acc, val) => acc ^ val, 0);
}
// 分块写入函数
async function writeMemory(address, dataChunk) {
    // 1. 发送写指令
    await sendCmd([0x31, 0xCE]);
    await waitAck();
    // 2. 发送 4 字节地址 + 地址的异或校验和
    let addrBytes = [
        (address >> 24) & 0xFF, 
        (address >> 16) & 0xFF, 
        (address >> 8) & 0xFF, 
        address & 0xFF
    ];
    addrBytes.push(calcXOR(addrBytes));
    await sendCmd(addrBytes);
    await waitAck();
    // 3. 发送数据长度 N (实际长度 - 1) + 数据本身 + 全部数据的异或校验和
    let N = dataChunk.length - 1;
    let dataPacket = [N, ...dataChunk];
    dataPacket.push(calcXOR(dataPacket)); // Checksum 包含 N 和数据
    await sendCmd(dataPacket);
    await waitAck();
}
// 主烧录循环 (假设 binFileArray 已经是固件的 Uint8Array)
let startAddress = 0x08000000;
for (let i = 0; i < binFileArray.length; i += 256) {
    let chunk = binFileArray.slice(i, i + 256);
    // ST 协议要求写入块必须是 4 字节对齐，如果最后一块不足，需补齐 0xFF
    while (chunk.length % 4 !== 0) {
        chunk = new Uint8Array([...chunk, 0xFF]);
    }
    await writeMemory(startAddress + i, chunk);
    console.log(`进度: ${Math.round((i / binFileArray.length) * 100)}%`);
}
```
### 功能 6：数据校验 (Verify / Read Memory)
与写入类似，只是换成读指令，读回来后与原本的 bin 文件做比对。
*   **行为**：发送 `0x11` + `0xEE`。
```javascript
async function readMemory(address, length) {
    // 1. 发指令
    await sendCmd([0x11, 0xEE]);
    await waitAck();
    // 2. 发地址及校验
    let addrBytes = [ /* 与 writeMemory 相同的地址切分和 XOR */ ];
    addrBytes.push(calcXOR(addrBytes));
    await sendCmd(addrBytes);
    await waitAck();
    // 3. 发送要读取的长度 N (length - 1) 及其反码
    let N = length - 1;
    await sendCmd([N, N ^ 0xFF]);
    await waitAck();
    // 4. 读取单片机返回的数据
    let readData = await readBytes(length);
    return readData;
}
```
---
## 第四部分：完整上位机调度总流程 (Master Sequence)
结合上述所有代码，当点击网页上的“开始编程”按钮时，主函数的生命周期应如下（**这正是 FlyMcu 内部的真实执行流程**）：
```javascript
async function startFlyMcuProcess(port, firmwareBuffer) {
    try {
        console.log("1. 正在复位单片机进入 ISP 模式...");
        await enterBootloader(port);
        console.log("2. 正在进行波特率同步...");
        await sendCmd([0x7F]);
        await waitAck();
        console.log("3. 正在获取芯片 ID...");
        // 伪代码: 执行 Get ID 命令，判断芯片合法性
        console.log("4. 尝试执行擦除...");
        try {
            // 先尝试发 0x44 扇区擦除指令
            // 如果捕获到 NACK(0x1F)，说明带读保护
        } catch (e) {
            console.warn("芯片处于读保护状态，正在解除保护并触发全片清除...");
            await unprotectAndMassErase();
            // 注意：解保护后会硬复位，需要重新进 Bootloader 并握手
            await enterBootloader(port);
            await sendCmd([0x7F]); await waitAck();
        }
        console.log("5. 开始烧录固件...");
        // 伪代码: 执行 writeMemory 分包循环
        console.log("6. 开始校验固件...");
        // 伪代码: 执行 readMemory 循环并进行比对
        console.log("7. 烧录成功，即将运行用户程序...");
        await resetToRun(port);
        console.log("🎉 所有操作完成！");
    } catch (error) {
        console.error("烧录失败，流程终止:", error);
    }
}
```
## 总结：开发检查清单 (Checklist)
1. [ ] **页面授权**：代码中调用 `navigator.serial.requestPort()` 必须包裹在一个 `<button onClick="..">` 事件中，否则浏览器会拦截。
2. [ ] **偶校验配置**：检查 `port.open({ baudRate: 115200, parity: "even" })`，漏掉 "even" 一定会通讯失败。
3. [ ] **缓冲机制**：浏览器的 `reader.read()` 是流式读取的，一次返回可能只有 1 个字节，读取函数必须有拼接和循环检测机制。
4. [ ] **字节对齐**：发送的 `.bin` 文件数据长度不足一包时，务必用 `0xFF` 填充，不要用 `0x00`（Flash 的默认擦除态是 `0xFF`）。
