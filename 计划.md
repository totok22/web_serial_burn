# Web MCU Burner 实现计划

## 目标

实现一个跨平台 STM32 串口 ISP 烧写工具：

- 浏览器端使用 Web Serial。
- CLI 端使用 Node.js `serialport`。
- 协议遵循 STM32 USART Bootloader AN3155。
- 自动完成进 Bootloader、擦除、烧写、校验和复位运行。

## 硬件模型

控制线用于进入和退出 Bootloader：

- DTR 控制 RESET。
- RTS 控制 BOOT0/ISP 选择。
- 项目内部约定：`true` 为低电平，`false` 为高电平。
- CH340C、CH340X 电路细节见 `docs/CH340_HARDWARE.md`。

经典 CH340C 电路已验证入口序列：

1. RTS 低电平。
2. DTR 低电平。
3. DTR 高电平。
4. 等待约 800ms。
5. 发送 `0x7F` 同步字节。

对照命令：

```bash
stm32flash -b 115200 -i -rts,-dtr,dtr /dev/tty.usbserial-10
```

退出并运行用户程序：

1. 设置 BOOT0 为运行态。
2. 脉冲 RESET。
3. 释放 DTR/RTS。
4. 延时等待端口关闭前稳定。

## 串口参数

- Baud rate: `115200`
- Data bits: `8`
- Stop bits: `1`
- Parity: `even`
- Flow control: `none`

## STM32 协议要点

- Sync: `0x7F`
- ACK: `0x79`
- NACK: `0x1F`
- 命令格式：`CMD, CMD ^ 0xFF`
- 地址包：4 字节地址 + XOR
- 写入包：`length - 1` + 数据 + XOR
- 写入数据需要 4 字节对齐，末尾用 `0xFF` 填充。

当前使用的命令：

- `0x00` GET
- `0x02` GET ID
- `0x11` READ MEMORY
- `0x31` WRITE MEMORY
- `0x43` ERASE
- `0x44` EXTENDED ERASE
- `0x92` READOUT UNPROTECT

## 固件格式

- `.bin`: 按配置地址写入，默认 `0x08000000`。
- `.hex`: 解析 Intel HEX 扩展地址记录，自动得到起始地址；空洞填充 `0xFF`。

## 流程

1. 打开串口。
2. DTR/RTS 进入 Bootloader。
3. 发送 `0x7F` 同步。
4. GET 和 GET ID 读取 Bootloader 信息。
5. 擦除 Flash。
6. 分块写入固件。
7. 读回校验。
8. 复位运行用户程序。

## 验证记录

已在 macOS + CH340C 经典电路 + STM32F10xxx Medium-density 上验证：

- 端口：`/dev/tty.usbserial-10`
- Bootloader: `0x22`
- PID: `0x0410`
- 固件：`PDM.hex`
- 结果：擦除、写入、校验完成。

CH340X 直连电路已抽象为 `ch340x` 自动时序，尚待硬件实测。
