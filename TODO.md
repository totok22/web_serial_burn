# 待办

## 高优先级

- 在同一块 CH340C 板子上验证浏览器烧写流程。
- 确认 macOS 和 Windows 烧写后的复位运行表现。
- 实测 `ch340x` 直连电路自动时序。
- 增加擦除、读取、写入、校验、解除读保护流程的协议测试。

## 中优先级

- 增加 CLI 自定义 DTR/RTS entry/exit 序列参数。
- 增加通用 DTR/RTS 时序参数，便于验证更多板型。
- Sync 失败时增加有限重试。
- 给 ACK 超时、NACK、用户程序残留输出增加明确错误提示。
- 增加常见 STM32 PID 表。
- 优化 CLI 进度输出，避免重复百分比刷屏。

## 低优先级

- 增加可选 `GO` 命令支持。
- 增加烧写日志导出。
- 增加基于模拟 Web Serial 的浏览器端冒烟测试。
